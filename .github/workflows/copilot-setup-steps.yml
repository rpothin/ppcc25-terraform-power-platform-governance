---
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
# GITHUB COPILOT SETUP STEPS WORKFLOW FOR POWER PLATFORM GOVERNANCE
# Configures the environment for GitHub Copilot coding agent to work with Power Platform Terraform governance.
#
# 🎯 WHY THIS EXISTS:
# - Provides Copilot coding agent with all required tools for Power Platform governance automation
# - Enables automated Terraform development, validation, and documentation tasks
# - Supports AVM-compliant module development and testing workflows
#
# 🔒 SECURITY DECISIONS:
# - Uses pinned tool versions for reproducible, secure environments
# - No hardcoded secrets - environment configured for secure operations
# - Modular tool installation via reusable shell scripts for consistency
#
# ⚙️ OPERATIONAL CONTEXT:
# - Manual dispatch for validation testing of Copilot environment setup
# - Auto-triggered on changes to this workflow for immediate validation
# - No timeout specified - relies on GitHub Actions default limits for tool installation
#
# 📋 INTEGRATION REQUIREMENTS:
# - Terraform ~> 1.12.2 for Power Platform provider compatibility (configurable via repository variables)
# - TFLint for Terraform code quality validation
# - terraform-docs for AVM-compliant module documentation
# - Python 3.11 with YAML processing tools (yamllint, PyYAML, actionlint)
# - Modular installation via scripts/utils/yaml-tools-installer.sh
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

name: "🛠️ Copilot Setup Steps"

concurrency:
  group: copilot-setup-steps
  cancel-in-progress: false

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

run-name: "🤖 Configure Copilot Environment by @${{ github.actor }}"

jobs:
  # The job MUST be called `copilot-setup-steps`
  # otherwise it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-latest

    # Permissions set just for the setup steps
    # Copilot has permissions to its branch
    
    permissions:
      # To allow GitHub to clone the repo for setup
      contents: read

    steps:
      # === REPOSITORY ACCESS ===
      # Required foundation for all Copilot operations
      - name: Checkout Repository
        uses: actions/checkout@v4

      # === TERRAFORM INFRASTRUCTURE ===
      # Primary tool for Power Platform governance automation
      - name: Setup Terraform CLI
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ vars.TERRAFORM_VERSION || '1.12.2' }}  # Uses repository variable with fallback
          terraform_wrapper: false       # WHY: Clean output for Copilot parsing

      # === TERRAFORM CODE QUALITY ===
      # Linting and validation for AVM-compliant modules
      - name: Install TFLint
        run: |
          curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
          tflint --version
          echo "TFLint installed for Terraform code quality validation"

      # === DOCUMENTATION TOOLS ===
      # Terraform documentation generation for AVM compliance
      - name: Install Documentation Tools
        run: |
          # Install terraform-docs for module documentation
          curl -sSLo ./terraform-docs.tar.gz https://terraform-docs.io/dl/v0.20.0/terraform-docs-v0.20.0-$(uname)-amd64.tar.gz
          tar -xzf terraform-docs.tar.gz
          chmod +x terraform-docs
          sudo mv terraform-docs /usr/local/bin/terraform-docs
          terraform-docs --version

      # === PYTHON ENVIRONMENT SETUP ===
      # Python 3.11 provides stable foundation for YAML processing libraries
      # No pip cache needed since tools are installed via shell scripts
      - name: "Setup Python Environment"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      # === YAML VALIDATION TOOLS INSTALLATION ===
      # Use modular installation library for consistent tool setup across environments
      # Leverages yaml-tools-installer.sh for reusable installation patterns
      - name: "Install YAML Validation Tools"
        run: |
          # Source the installation library and install all validation tools
          source scripts/utils/yaml-tools-installer.sh
          install_all_yaml_tools
          
          # Ensure PATH includes user-installed packages for subsequent steps
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      # === ENVIRONMENT VALIDATION ===
      # Comprehensive validation that all tools are properly installed
      - name: Validate Environment Setup
        run: |
          echo "=== Environment Validation ==="
          echo "Validating all tools for Copilot coding agent..."
          
          # Core tools validation
          echo "✓ Terraform: $(terraform version -json | jq -r '.terraform_version')"
          echo "✓ TFLint: $(tflint --version | head -1)"
          echo "✓ Azure CLI: $(az version --query '"azure-cli"' -o tsv)"
          echo "✓ Power Platform CLI: $(pac --version | head -1)"
          echo "✓ NPM: $(npm --version)"
          
          # Documentation tools validation  
          echo "✓ terraform-docs: $(terraform-docs version)"
          echo "✓ yamllint: $(yamllint --version)"
          echo "✓ actionlint: $(actionlint -version)"
          echo "✓ Python: $(python3 --version)"
          echo "✓ PyYAML: $(python3 -c 'import yaml; print(yaml.__version__)')"
          
          # Repository context validation
          echo "✓ Repository: ${GITHUB_REPOSITORY}"
          echo "✓ Branch: ${GITHUB_REF_NAME}"
          echo "✓ Workspace: ${GITHUB_WORKSPACE}"
          
          # Power Platform governance context
          echo "✓ Configurations directory: $(ls -la configurations/ | wc -l) items"
          echo "✓ Scripts directory: $(ls -la scripts/ | wc -l) items"
          echo "✓ Documentation: $(ls -la docs/ | wc -l) items"
          
          echo "=== Environment Ready ==="
          echo "GitHub Copilot coding agent environment configured successfully!"
          echo "All tools installed and validated for Power Platform governance automation."

      # === PERFORMANCE OPTIMIZATION ===
      # Cache setup for faster subsequent Copilot runs
      - name: Setup Caching
        uses: actions/cache@v4
        with:
          path: |
            ~/.terraform.d/plugin-cache
            ~/.cache/pip
            ~/.npm
          key: copilot-setup-${{ runner.os }}-${{ hashFiles('**/*.tf', '**/package*.json', '**/requirements*.txt') }}
          restore-keys: |
            copilot-setup-${{ runner.os }}-