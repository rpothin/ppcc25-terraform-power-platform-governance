# ==============================================================================
# Configuration Template for Power Platform Terraform Governance Setup
# ==============================================================================
# Copy this file to config.env and fill in your specific values
# The config.env file is excluded from version control for security
# ==============================================================================

# GitHub Repository Configuration
# Required: Your GitHub repository in owner/repo format
GITHUB_OWNER="your-github-username"
GITHUB_REPO="your-repo-name"

# Azure Configuration
# Optional: Will use current Azure CLI context if not specified
AZURE_SUBSCRIPTION_ID=""
AZURE_TENANT_ID=""

# Service Principal Configuration
# Optional: Will generate default name if not specified
SP_NAME="terraform-powerplatform-governance"

# Terraform Backend Configuration
# Optional: Will use defaults if not specified
RESOURCE_GROUP_NAME="rg-terraform-powerplatform-governance"
STORAGE_ACCOUNT_NAME=""  # Will auto-generate if empty
CONTAINER_NAME="terraform-state"
LOCATION="East US"

# ==============================================================================
# Instructions:
# 1. Copy this file: cp config.env.example config.env
# 2. Edit config.env with your specific values
# 3. Run: ./setup.sh
# ==============================================================================

# Security Notes:
# - config.env is excluded from version control (.gitignore)
# - No sensitive credentials are stored in this file
# - Authentication uses OIDC (no long-lived secrets)
# - All values are public identifiers safe for local storage
